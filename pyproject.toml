[project]
name = "ecommerce-cs-ai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "MutiaraSianturi", email = "mutiarasianturi006@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ecommerce-cs-ai-agent = "ecommerce_cs_ai_agent:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
